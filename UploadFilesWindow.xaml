<Window x:Class="_01electronics_crm.UploadFilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_01electronics_crm"
        mc:Ignorable="d"
        Title="UploadFilesWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                
                <RowDefinition Height="1*"/>
                <RowDefinition Height="0.2*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Background="#105A97" Name="uploadFilesHeader" Content="UPLOAD FILES WINDOW" Style="{StaticResource secondaryHeaderTextStyle}"/>

            
            <StackPanel Grid.Row="1" Name="uploadFilesStackPanel" Drop="OnDropUploadFilesStackPanel" AllowDrop="True"  Background="White" Margin="40,10,47,68"/>
            <ProgressBar Grid.Row="2" Name="progressBar" Visibility="Collapsed" Style="{StaticResource ProgressBarStyle}" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
